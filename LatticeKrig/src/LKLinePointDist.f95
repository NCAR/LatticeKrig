subroutine LKLinePointDist(dim, points, nPoints, lines, nLines, ranges, rangeReps, nRanges, output)
    implicit none
    integer dim, nPoints, nLines, nRanges, rangeReps(nRanges)
    double precision points(dim, nPoints), lines(dim, nLines), ranges(nRanges)
    
end subroutine LKLinePointDist